# SimpleOKM API credentials - TEMPLATE. This file IS committed; copy it to credentials.cfg
# (which is gitignored) and replace the dummy values with real ones.
# Format: KEY=VALUE, one per line; '#' starts a comment.

# The Odnoklassniki application key and secret to authenticate with.
OK_APP_KEY=XXXXXXXXXXXXXXXX
OK_APP_SECRET=your_app_secret_here

# The User-Agent sent with every request; match it to the client whose key you use.
OK_USER_AGENT=OK/0.0 (Example Device; ru)
